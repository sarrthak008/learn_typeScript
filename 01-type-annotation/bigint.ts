//in js we get maximum big int using 
// we get is value in < ES2020; // for these search targer == exVersion on config-file

let BigIntergerInJS:bigint = BigInt("900719925474099144");
let maxNum = (Number as any).MAX_SAFE_INTEGER;
console.log(maxNum);